import type { Config } from "tailwindcss";

const config: Config = {
  theme: {
    extend: {
      colors: {
        primary: "#4f46e5",
        secondary: "#06b6d4",
      },
    },
  },
};

export default config;
